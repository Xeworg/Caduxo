# Tasks — calendar-month-picker

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~830 net total — ~230 human-edited (`CalendarMonth.svelte` ~70 net, two i18n catalogs ~10 total, canonical spec delta ~150) + ~80 regenerated `i18n-types.ts` + this `tasks.md` + the `apply-progress.md` evidence ledger |
| Project review budget | 800 changed lines (`openspec/config.yaml` → `sdd.reviewBudgetChangedLines`) |
| Session review budget | 3000 changed lines (session preflight) |
| 400-line budget risk | Low — the human-edited delta is comfortably under the 400-line canonical threshold even before regenerated types are excluded, and the regenerated `i18n-types.ts` follows the `caduxo-daisyui-redesign` precedent of being treated as out-of-budget for review purposes |
| Chained PRs recommended | No — single PR is sufficient; design §"File changes" already records the i18n-then-Svelte split contingency only as a budget escape hatch, not as a default |
| Suggested split | Single PR — `CalendarMonth.svelte` + i18n catalogs + regenerated types + canonical spec delta + evidence ledger |
| Delivery strategy | `auto-chain` (session preflight); no chain selected because single PR is within both budgets |
| Chain strategy | N/A (single PR; no chain required) |
| Strict TDD | `false` (project default; project has no frontend test harness for `CalendarMonth.svelte`) |

```text
Decision needed before apply: No
Chained PRs recommended: No
Chain strategy: N/A
400-line budget risk: Low
```

The forecast is the human-edited delta is well under both the project's 800-line ceiling and the session's 3000-line ceiling. The proposal question round is closed (D1–D12 locked in `design.md > Confirmed scope boundaries`); no product question round is required before this `tasks.md` or before apply. The orchestrator may proceed straight from `tasks.md` → `apply-progress.md` → verify report → archive without asking the user any new question.

---

## Slice 1 — Pre-flight: anchor the diff to today's line numbers

Goal: confirm the file's current state matches the line numbers cited in `design.md` before any edit lands, so the work-unit commit messages can cite exact lines and reviewers can audit the diff without a stale snapshot.

- [x] Read `src/components/CalendarMonth.svelte` end-to-end and confirm the anchors cited in `design.md > Component design > Current structure (anchor for the diff)`: `let showYearPicker = false;` near line 97, `let pickerYear: number = viewYear;` near line 98, `function prevMonth()` / `nextMonth()` near lines 159–160, `function cycleMonth()` at line 160 (TO BE REMOVED), `function openYearPicker()` near lines 168–171, `function onKeydown(e)` near line 259 with the `if (showYearPicker) { … return; }` early-return guard, the `{#if showYearPicker}` markup branch near line 342, and the default-branch `<Button aria-label={$LL.calendar.ariaCycleMonth()} onclick={cycleMonth}>` markup near line 400–404. Record any drift from the design's line numbers inside `apply-progress.md` so the apply-phase review can reconcile. <!-- sdd-owner: implementation -->
- [x] Run `grep -RIn 'ariaCycleMonth' src/` from `/home/xeworg/Proyectos/Caduxo` and confirm the only runtime hits are `src/components/CalendarMonth.svelte:403` (to be replaced), `src/i18n/en/index.ts:719`, `src/i18n/es/index.ts:719`, and the regenerated `src/i18n/i18n-types.ts`. Record the baseline output inside `apply-progress.md` so the post-apply grep-gate can be compared verbatim. <!-- sdd-owner: implementation -->
- [x] Run `grep -RIn 'cycleMonth' src/` from `/home/xeworg/Proyectos/Caduxo` and confirm the only hit is `src/components/CalendarMonth.svelte` (declaration at line 160, call site at line 404). Record the baseline output inside `apply-progress.md`. <!-- sdd-owner: implementation -->
- [x] Initialize `openspec/changes/calendar-month-picker/apply-progress.md` with the file-scope summary (one Svelte component edit + two i18n catalogs + regenerated types + canonical spec delta), the design §"File changes" per-file LOC estimate, the locked D1–D12 decision table from `design.md > Confirmed scope boundaries`, and the M1–M20 manual-smoke checklist from `design.md > Validation plan > Manual smoke` as an unfilled template. This file is the apply-phase evidence ledger; it must exist before any component lands so evidence can be appended as work progresses. <!-- sdd-owner: implementation -->

## Slice 2 — i18n surface: rename + new keys + regenerate types

Goal: lock the i18n contract first so the regenerated `i18n-types.ts` carries the new keys before any Svelte file references them; this keeps `svelte-check` green at every intermediate state and avoids a one-shot mass-edit.

- [x] In `src/i18n/en/index.ts`, remove the `ariaCycleMonth: "Cycle month",` entry from the `calendar.*` block (currently at line 719) and add five new entries in the same block: `ariaOpenMonthPicker: "Open month picker",`, `ariaCloseMonthPicker: "Close month picker",`, `ariaPreviousYear: "Previous year",`, `ariaNextYear: "Next year",`, `ariaMonth: "{month} {year}",`. Preserve the existing alphabetical / grouping ordering already used by the `calendar` block so the diff stays mechanical. <!-- sdd-owner: implementation -->
- [x] In `src/i18n/es/index.ts`, remove the `ariaCycleMonth: "Cambiar mes",` entry (currently at line 719) and add the same five new entries with the Spanish strings from `design.md > i18n surface`: `ariaOpenMonthPicker: "Abrir selector de mes",`, `ariaCloseMonthPicker: "Cerrar selector de mes",`, `ariaPreviousYear: "Año anterior",`, `ariaNextYear: "Año siguiente",`, `ariaMonth: "{month} {year}",`. <!-- sdd-owner: implementation -->
- [x] Run `npm run i18n:generate` from `/home/xeworg/Proyectos/Caduxo`; confirm it exits 0 and that `src/i18n/i18n-types.ts` now carries `ariaOpenMonthPicker`, `ariaCloseMonthPicker`, `ariaPreviousYear`, `ariaNextYear`, `ariaMonth` under the `calendar` namespace and no longer carries `ariaCycleMonth`. The `ariaMonth` key MUST be generated as a function accepting `{ month: string; year: number }` (the `typesafe-i18n` parameter shape for `RequiredParams<'month' | 'year'>`). <!-- sdd-owner: implementation -->
- [x] Run `npm run check` from `/home/xeworg/Proyectos/Caduxo`; it exits 0 even though `CalendarMonth.svelte` still references `ariaCycleMonth` (because the old function is still in the regenerated types until the catalog edits drop it — this step validates only that the regeneration did not produce a malformed types file, NOT that all runtime references are migrated). Record the exit code inside `apply-progress.md`. <!-- sdd-owner: implementation -->

## Slice 3 — `CalendarMonth.svelte` state + handlers

Goal: introduce the new local state and handlers in the `<script>` block before any markup change, so the markup edit only has to reference already-declared symbols.

- [x] Add `let showMonthPicker = false;` next to the existing `let showYearPicker = false;` (near line 97) and add the `openMonthPicker()` / `closeMonthPicker()` / `selectMonth(m: number)` / `prevPickerYear()` / `nextPickerYear()` / `isMonthInRange(year: number, month: number): boolean` / `onMonthChipKeydown(e: KeyboardEvent)` functions per `design.md > Component design > New handler functions` and `design.md > Component design > Keyboard surface`. Place them next to the existing `cycleMonth()` declaration so the diff stays mechanical; `cycleMonth()` itself is removed in the next task. The `prevPickerYear` / `nextPickerYear` clamps mirror the existing 1900–2100 range already used elsewhere in the calendar surface. <!-- sdd-owner: implementation -->
- [x] Delete the `function cycleMonth() { … }` declaration (currently at line 160) entirely. Do NOT keep a `Shift+click` power gesture, a deprecated stub, or a re-export — `design.md > Confirmed scope boundaries` D7 retires it completely and `design.md > Risks and mitigations` R8 documents that no external file references the symbol. <!-- sdd-owner: implementation -->
- [x] Extend `function openYearPicker()` so it ALSO sets `showMonthPicker = false;` (mutual exclusion per `design.md > Mutual exclusion` and D10). The reverse case (`openMonthPicker()` already sets `showYearPicker = false`) is in the previous task. <!-- sdd-owner: implementation -->
- [x] Extend `function onKeydown(e: KeyboardEvent)` (near line 259) so the early-return guard covers both overlays: `if (showYearPicker || showMonthPicker) { if (e.key === "Escape") { showYearPicker = false; showMonthPicker = false; e.preventDefault(); } return; }`. The remaining `switch (e.key) { … }` block (arrow / PageUp / PageDown / Shift+PageUp / Shift+PageDown / Enter / Escape branches) is UNCHANGED so the existing keyboard-surface scenario continues to pass verbatim. <!-- sdd-owner: implementation -->
- [x] Behaviour check — `npm run check` exits 0 after the script-block edits; `svelte-check` must NOT report missing-key diagnostics for `ariaOpenMonthPicker`, `ariaCloseMonthPicker`, `ariaPreviousYear`, `ariaNextYear`, or `ariaMonth`. Record the exit code and any key-related output inside `apply-progress.md`. <!-- sdd-owner: implementation -->

## Slice 4 — `CalendarMonth.svelte` markup: month label trigger + overlay branch

Goal: convert the default-branch `<Button>` to the new trigger, insert the month picker overlay branch between the existing year picker branch and the default branch, and keep the day grid / weekday header / badge dots / footer slot untouched.

- [x] In the default header branch (currently lines 400–407), replace `aria-label={$LL.calendar.ariaCycleMonth()}` with `aria-label={$LL.calendar.ariaOpenMonthPicker()}` and replace `onclick={cycleMonth}` with `onclick={openMonthPicker}`. The visible label (`{MONTH_NAMES[viewMonth - 1] ?? ""}`), the `variant` / `size` props, and the surrounding chevrons are UNCHANGED. <!-- sdd-owner: implementation -->
- [x] Insert a new `{:else if showMonthPicker}` branch between the existing `{#if showYearPicker} …` block and the `{:else}` default branch, following the markup in `design.md > Component design > Markup changes (header)`: the year-chip button (still clickable per D10, reuses `openYearPicker()` so opening year picker closes month picker), the `.month-picker` wrapper with `role="dialog"` and `aria-label={$LL.calendar.ariaCloseMonthPicker()}`, the `.month-picker-header` row with prev-year chevron / `pickerYear` label / next-year chevron (each chevron uses `<Tooltip>` + `<Button variant="ghost" size="sm">` with the matching `$LL.calendar.ariaPreviousYear()` / `$LL.calendar.ariaNextYear()` aria-labels), and the `.month-grid` `role="grid"` containing the 12 `<button class="month-chip">` elements driven by `{#each MONTH_NAMES as monthName, i (i)}` with `{@const m = i + 1}`, `{@const inRange = isMonthInRange(pickerYear, m)}`, `{@const isSelected = pickerYear === viewYear && m === viewMonth}`. The chip wiring MUST set `aria-pressed={isSelected}`, `aria-disabled={!inRange}`, `tabindex={isSelected ? 0 : -1}`, `disabled={!inRange}`, `on:click={() => inRange && selectMonth(m)}`, `on:keydown={onMonthChipKeydown}`, and `aria-label={$LL.calendar.ariaMonth({ month: monthName, year: pickerYear })}`. The visible label is `{monthName}`. <!-- sdd-owner: implementation -->
- [x] Confirm the existing `{#if showYearPicker}` branch's chevron structure is NOT modified — only the new `{:else if showMonthPicker}` branch is added. Confirm the day grid `<div class="cal-grid">` and the footer `<slot name="footer" />` are untouched. <!-- sdd-owner: implementation -->
- [x] Behaviour check — `npm run check` exits 0; no Svelte `a11y_*` warning for the `<button class="month-chip">` `role="grid"` placement, no `unhandled-rejection` from the `ariaMonth` parameter shape, no missing-key diagnostics. Record the exit code inside `apply-progress.md`. <!-- sdd-owner: implementation -->

## Slice 5 — `CalendarMonth.svelte` CSS: mirror the year picker rules

Goal: add the `.month-picker` / `.month-picker-header` / `.picker-year-label` / `.month-grid` / `.month-chip` / `.month-chip.month-selected` / `.month-chip.month-disabled` / `.month-chip:focus-visible` rules inside the component-scoped `<style>` block, mirroring `.year-picker*` one-for-one with the documented deltas (240px width vs 220px, `repeat(3, 1fr)` vs `repeat(4, 1fr)`, `padding: 6px 2px` vs `4px 2px`). No global CSS edit; no new theme tokens.

- [x] Append the eight CSS rules listed in `design.md > Component design > Visual classes (CSS)` to the component-scoped `<style>` block in `src/components/CalendarMonth.svelte`. Reuse the existing theme tokens (`--color-base-100`, `--color-base-200`, `--color-base-300`, `--color-base-content`, `--color-primary`) without introducing new variables. The `position: absolute; top: calc(100% + 4px); left: 50%; transform: translateX(-50%); z-index: 10;` anchoring math is IDENTICAL to `.year-picker` so `DatePicker.svelte`'s `positionPopover()` math stays untouched (D6 / `design.md > Risks and mitigations` R1). <!-- sdd-owner: implementation -->
- [x] Confirm `src/components/CalendarPage.svelte` and `src/components/DatePicker.svelte` are NOT modified — the design verified (D6) that neither consumer needs an edit because the dispatch surface and props stay identical. <!-- sdd-owner: implementation -->

## Slice 6 — Canonical spec delta (soft sync inside apply)

Goal: apply the textual `## MODIFIED Requirements` and `## ADDED Requirements` blocks from `openspec/changes/calendar-month-picker/specs/caduxo-expiry-tracker/spec.md` to the canonical spec at `openspec/specs/caduxo-expiry-tracker/spec.md`. The design documents (`design.md > Canonical spec delta (locked from spec.md)`) that the proposal's line-number references are stale post-`2026-09-20-caduxo-daisyui-redesign`; the apply step uses the textual delta block, not a `sed` on a line number.

- [x] Apply the `## MODIFIED Requirements > Requirement: Calendar tab` block from the change-scoped spec at `openspec/changes/calendar-month-picker/specs/caduxo-expiry-tracker/spec.md` to BOTH occurrences of the "Calendar tab" requirement in the canonical spec at `openspec/specs/caduxo-expiry-tracker/spec.md`. The block updates the tab-order wording (month label is a **trigger for the month picker**, not a `cycleMonth` button), updates the keyboard-surface scenario to require `Enter` / `Space` on the month label opens the picker without changing `viewMonth`, and appends the new "clicking the month label opens a month picker grid" scenario. The block does NOT change any non-calendar paragraph. <!-- sdd-owner: implementation -->
- [x] Append the `## ADDED Requirements > Requirement: Calendar month picker` block from the change-scoped spec to the canonical spec, immediately after the modified "Calendar tab" requirement. The block codifies the overlay (header + 3×4 grid), the keyboard surface (Esc / arrow keys / Enter / Space / Tab), the disabled / selected rules, the mutual exclusion with the year picker, the i18n surface, the retirement of `cycleMonth()`, and the shared behaviour between Calendar tab and `DatePicker`. <!-- sdd-owner: implementation -->
- [x] Confirm the textual delta is applied via OpenSpec tooling (or a manual block-level replacement that preserves the modified-block structure), NOT via a literal `sed -i 's/ariaCycleMonth/ariaOpenMonthPicker/g'` on the canonical spec — `design.md > Risks and mitigations` R10 documents that the canonical spec at lines 1235 / 2748–2750 already lacks the `ariaCycleMonth` string post-redesign. Record the actual line-count delta (`git diff --stat openspec/specs/caduxo-expiry-tracker/spec.md`) inside `apply-progress.md`. <!-- sdd-owner: implementation -->

## Slice 7 — Automated verify gate

Goal: run the four mechanical gates that confirm the slice did not break type-check, build, or naming invariants.

- [x] Run `npm run i18n:generate` from `/home/xeworg/Proyectos/Caduxo`; exit code 0; the regenerated `src/i18n/i18n-types.ts` carries the five new `calendar.*` function keys and does NOT carry `ariaCycleMonth`. Record the exit code and any diff stat (`git diff --stat src/i18n/i18n-types.ts`) inside `apply-progress.md`. <!-- sdd-owner: implementation -->
- [x] Run `npm run check` from `/home/xeworg/Proyectos/Caduxo`; exit code 0; no missing-key diagnostics emitted by `typesafe-i18n`; no Svelte `a11y_*` warning introduced by the new markup. Record the exit code and any output mentioning `CalendarMonth`, `DatePicker`, `CalendarPage`, or `App.svelte` inside `apply-progress.md`. <!-- sdd-owner: implementation -->
- [x] Run `npm run build` from `/home/xeworg/Proyectos/Caduxo`; exit code 0; `prebuild` hook regenerates `i18n-types.ts` and `vite build` exits green. Record the exit code and the bundle-size delta inside `apply-progress.md`. <!-- sdd-owner: implementation -->
- [x] Run `grep -RIn 'ariaCycleMonth' src/` from `/home/xeworg/Proyectos/Caduxo`; expected output: zero matches (the old key is gone from runtime code and from both i18n catalogs). Record the actual output inside `apply-progress.md` and treat any non-zero match as a blocker for the next task. <!-- sdd-owner: implementation -->
- [x] Run `grep -RIn 'cycleMonth' src/` from `/home/xeworg/Proyectos/Caduxo`; expected output: zero matches. Record the actual output inside `apply-progress.md` and treat any non-zero match as a blocker for the next task. <!-- sdd-owner: implementation -->
- [x] Run `grep -RIn 'type="date"' src/` from `/home/xeworg/Proyectos/Caduxo`; expected output: zero matches (regression check that the `2026-09-15-caduxo-custom-date-picker` defect fix is still in effect). Record the actual output inside `apply-progress.md`. <!-- sdd-owner: implementation -->

## Slice 8 — Manual smoke matrix (Calendar tab + DatePicker popover + i18n + range)

Goal: exercise every spec scenario end-to-end inside `npm run tauri dev` on the same shell as `2026-09-15-caduxo-custom-date-picker` so the change can be archived with a receipt. The 20-step matrix in `design.md > Validation plan > Manual smoke` is the canonical checklist; record the outcome (`pass` / `fail` / `partial` + the actual observed value) for each row inside `apply-progress.md > Manual smoke`. Steps that require a desktop runtime (display server + WebKitGTK or WebView2) are marked `[ ]` because they can only be ticked in the verify phase, not from a headless shell.

- [x] M1 — Calendar tab opens at current month; month label reads e.g. "September", year chip reads "2026 ▼", no overlay open. <!-- sdd-owner: implementation -->
- [x] M2 — Calendar tab click on month label opens the month picker overlay anchored under the header, shows 12 month chips in a 3×4 grid, the current-viewed-month chip is highlighted with `.month-selected`, and the picker header year matches `viewYear`. <!-- sdd-owner: implementation -->
- [x] M3 — Calendar tab click on `›` inside the picker header advances the picker-year label by 1 (clamped at 2100); the chips re-evaluate selected/disabled; the calendar header still shows the original `viewYear`. <!-- sdd-owner: implementation -->
- [x] M4 — Calendar tab click on an enabled month chip closes the overlay, updates the month label, rebuilds the day grid, and updates the day-detail panel for the new (year, month). <!-- sdd-owner: implementation -->
- [x] M5 — Calendar tab click on the month label then `Esc` closes the overlay; `viewMonth` / `viewYear` / `selectedDate` are unchanged. <!-- sdd-owner: implementation -->
- [x] M6 — Calendar tab tab order traverses `prev chevron → month label → year chip → day grid → next chevron`; Tab from prev chevron focuses the month label, second Tab focuses the year chip, third Tab focuses the first day cell. <!-- sdd-owner: implementation -->
- [x] M7 — Calendar tab with picker open: `ArrowLeft` / `ArrowRight` / `ArrowUp` / `ArrowDown` move focus through the 3×4 grid; disabled chips are skipped. <!-- sdd-owner: implementation -->
- [x] M8 — Calendar tab with picker open: `Enter` on a focused, enabled chip closes the overlay, dispatches `monthChange`, and updates the day grid. <!-- sdd-owner: implementation -->
- [x] M9 — Calendar tab: clicking the year chip while the month picker is open closes the month picker and opens the year picker (mutual exclusion). <!-- sdd-owner: implementation -->
- [x] M10 — Calendar tab: clicking the month label while the year picker is open closes the year picker and opens the month picker (mutual exclusion). <!-- sdd-owner: implementation -->
- [x] M11 — Calendar tab: clicking the prev chevron (`‹`) when the picker is closed navigates backward by one month; `monthChange` dispatched; picker stays closed. <!-- sdd-owner: implementation -->
- [x] M12 — Calendar tab: pressing `PageUp` / `PageDown` when the picker is closed navigates by one month; picker stays closed. <!-- sdd-owner: implementation -->
- [x] M13 — Calendar tab: pressing `Shift+PageUp` / `Shift+PageDown` when the picker is closed navigates by one year (month clamped); picker stays closed. <!-- sdd-owner: implementation -->
- [x] M14 — `DatePicker` popover: opening the popover and clicking the month label renders the same overlay, the same grid, and the same selection semantics as the Calendar tab. <!-- sdd-owner: implementation -->
- [x] M15 — `DatePicker` popover: picking a month in the picker updates the popover preview to the new month via the consumer's existing `onMonthChange` handler (`popoverYear` / `popoverMonth` re-assigned). <!-- sdd-owner: implementation -->
- [x] M16 — `DatePicker` popover: the popover stays anchored to the trigger's bounding rect; the month picker overlay sits inside the `CalendarMonth.svelte` header (no popover anchor change, no `positionPopover()` math edit). <!-- sdd-owner: implementation -->
- [x] M17 — i18n: switching the active locale to `es` in Configuration renders the month label aria-label as `Abrir selector de mes`; close / prev-year / next-year aria-labels render in Spanish; month chips render Spanish month names from `MONTH_NAMES`. <!-- sdd-owner: implementation -->
- [x] M18 — Range boundary: a `DatePicker` instance configured with a custom `minDate` / `maxDate` tighter than the default range renders out-of-range months with `.month-disabled`; clicking a dimmed chip does nothing; clicking an enabled chip selects and closes. <!-- sdd-owner: implementation -->
- [x] M19 — Range boundary: default range (`1900-01-01` to `2100-12-31`) renders all 12 months as enabled, including all 12 in `pickerYear = 1900` and `pickerYear = 2100` (the `2100-12-30` corner case is intentionally still enabled because day 31 of December 2100 is inside the range). <!-- sdd-owner: implementation -->
- [x] M20 — Keyboard focus restore: open the month picker, close via `Esc`, press `Tab`; focus returns to the day grid (the chip that was last focused before opening, or the day cell that owns `focusedIso`). <!-- sdd-owner: implementation -->

## Slice 9 — Verify report + archive

Goal: write the parent gate input and close out the SDD lifecycle so the change can move to `openspec/changes/archive/<date>-calendar-month-picker/`.

- [x] Write the verify report at `openspec/changes/calendar-month-picker/verify-report.md` summarising: the three automated gates (`i18n:generate`, `check`, `build`), the four grep gates (the two M-grep gates + the two pre-existing regression greps `type="date"` and `ariaCycleMonth` / `cycleMonth`), the M1–M20 manual-smoke outcomes, and the spec-scenario coverage matrix from `openspec/changes/calendar-month-picker/specs/caduxo-expiry-tracker/spec.md` marked `pass` / `fail` / `partial` per scenario. Cross-link to `apply-progress.md` as the evidence ledger. The verify report is the parent gate's input; it must reference every checklist item above with the concrete evidence (command + exit code + observed output). <!-- sdd-owner: implementation -->

---

## Parent actions (lifecycle gates and review)

- [x] Before apply: confirm the single-PR shape (no chain) and that the forecasted ~230 human-edited LOC delta stays under the 400-line canonical review threshold during apply. If the apply-time `git diff --stat` for `CalendarMonth.svelte` + i18n catalogs + canonical spec delta exceeds 400 net lines, escalate via `deliveryStrategy: ask-on-risk` and consider the design's contingency (split into PR 1 i18n rename and PR 2 Svelte change) before merging. Do not invent a `size:exception` without explicit user acceptance. <!-- sdd-owner: parent -->
- [ ] Open PR against `main` once the Slice 7 automated gates are green and the Slice 8 manual smoke matrix has been recorded inside `apply-progress.md`; the PR description MUST cross-reference the D1–D12 decision table from `design.md > Confirmed scope boundaries` and the canonical-spec delta applied in Slice 6. <!-- sdd-owner: parent -->
- [ ] Start or reuse a bounded review for the change covering at minimum: (a) the mutual-exclusion rule between `showMonthPicker` and `showYearPicker`, (b) the keyboard surface inside the picker (Esc / arrow / Enter / Space / Tab), (c) the disabled-rule correctness against the spec's "at least one day in range" definition (including the `2100-12-30` corner case), (d) the i18n parity between `en` and `es` (including the `{month} {year}` parameter shape on `ariaMonth`), (e) the consumer parity between `CalendarPage.svelte` and `DatePicker.svelte` (no consumer edits, identical dispatch semantics), (f) the canonical-spec delta applied to BOTH occurrences of the "Calendar tab" requirement and the new "Calendar month picker" requirement added cleanly, (g) the regression greps (`type="date"` still zero, `cycleMonth` and `ariaCycleMonth` both zero). <!-- sdd-owner: parent -->
- [ ] After the PR lands and review passes: archive the SDD artifacts (`proposal.md`, `specs/...`, `design.md`, this `tasks.md`, `apply-progress.md`, `verify-report.md`) per the project's OpenSpec lifecycle into `openspec/changes/archive/<date>-calendar-month-picker/`; mark the change `applied` and close out the `sdd/calendar-month-picker/*` Engram topic keys once the bounded review receipt is filed. <!-- sdd-owner: parent -->
