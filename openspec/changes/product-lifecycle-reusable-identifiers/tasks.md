# Tasks — product-lifecycle-reusable-identifiers

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~980 net total — PR 1 backend ≈ ~420 (V19 migration + 12 V19 tests + repository lifecycle helpers + service layer `archive`/`unarchive`/`retire`/`list_lifecycle_events` + transactional helper + scanner/CSV/backup/message/DTO/IPC wiring + tests), PR 2 UI + i18n ≈ ~420 (`ProductCatalogPage` `showRetired` toggle + bottom-sort + badge, `ProductDetailPage` retire + unarchive + lifecycle history + conditional banner, `ProductForm` read-only lifecycle field, `DashboardPage` retired badge + retire-aware quick-create, `CsvImportPage` released-status badges + counters, `lib/products.ts` typed wrappers, full i18n parity on `en` + `es`), PR 3 verify-report + review fixes ≈ ~140 |
| Project review budget | 800 changed lines (`openspec/config.yaml` → `sdd.reviewBudgetChangedLines`) |
| Session review budget | 3000 changed lines (session preflight) |
| **400-line canonical budget** | High — full feature is ~123% of the canonical 400-line threshold; per-PR risk is **Low for PR 1** at ~420 (at the upper edge), **Medium/High for PR 2** at ~420 (at the upper edge; design §10 reserves a 2a/2b split escape hatch), Low for PR 3 at ~140 |
| Chained PRs recommended | **Yes** — `auto-chain` delivery strategy with `stacked-to-main` chain strategy confirmed by the parent. The single-PR shape exceeds the canonical 400-line threshold; the project already used `stacked-to-main` for `scanner-quick-operations` (same backend → UI → verify-report shape) and `caduxo-i18n-support`, so the precedent is in place |
| Suggested split | **PR 1 — Backend foundation**: V19 migration (`products` + `product_barcodes` rebuild with `lifecycle` column + partial unique indexes + `product_lifecycle_events` audit table) + repository helpers + service-layer `unarchive_product` / `retire_product` / `list_lifecycle_events` + transactional `archive_product` refactor + scanner / CSV / backup / user-messages / DTO / IPC wiring + tests. **PR 2 — UI + i18n parity**: catalog `Show retired` toggle + retired badge + bottom-sort, detail page retire + unarchive + lifecycle history + conditional banner, form read-only lifecycle field, dashboard retired badge + retire-aware quick-create, CSV import page released-status badges, `lib/products.ts` wrappers, full i18n parity on `en` + `es`. **PR 3 — Verification + review fixes**: `verify-report.md`, M1–M16 manual smoke, grep / regression gates, test fixes discovered during PR 1 / PR 2 review |
| Delivery strategy | `auto-chain` (session preflight; parent explicitly confirmed) |
| Chain strategy | `stacked-to-main` (parent explicitly confirmed) — PR 1 lands to `main`, PR 2 stacks onto PR 1's branch and lands once PR 1 is merged, PR 3 stacks onto PR 2's branch and lands once PR 2 is merged |
| Strict TDD | `false` (project default); lifecycle service tests are strongly encouraged but not TDD-mandatory. RED/GREEN evidence below is reported as `not active` |
| Native review candidate | The first work-unit commit per PR, not the accumulated feature branch |
| Mid-PR escalation | If PR 1's `git diff --stat` exceeds 400 net lines during implementation, escalate via `deliveryStrategy: ask-on-risk` and split into PR 1a (schema + repository helpers + service-layer lifecycle methods + audit) and PR 1b (DTOs, IPC wiring, CSV preview, backup/restore, message variants). If PR 2's diff exceeds 400 net lines, split into PR 2a (catalog / detail / dashboard / form / `lib/products.ts` / i18n keys) and PR 2b (CSV import page only — wire to the backend contract shipped in PR 1). |

```text
Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: High
```

The full feature is structurally above the 400-line canonical threshold; `auto-chain` is non-optional. The chain strategy is `stacked-to-main` per the parent preflight and matches the `scanner-quick-operations` precedent. The orchestrator confirms PR 1 / PR 2 sub-split triggers before each apply.

### Design constraints to preserve at every slice

These constraints come from `design.md §2.1`, `§3.2`, and `§13` and must be respected by every task below. The slice descriptions cite them inline so the implementation does not regress.

1. **SQLite partial-index single-table predicate.** Both partial UNIQUE indexes (`uq_products_sku_active`, `uq_product_barcodes_barcode_active`) MUST use predicates that reference ONLY columns of the indexed table (`products(sku) WHERE lifecycle != 'retired'` and `product_barcodes(barcode) WHERE lifecycle != 'retired'`). SQLite's parser rejects cross-table `WHERE EXISTS (…)` forms in partial-index predicates.
2. **`product_barcodes.lifecycle` mirrors `products.lifecycle` service-layer invariant.** Every transition (`archive`, `unarchive`, `retire`) writes `products.lifecycle`, the sibling mirror UPDATE on `product_barcodes.lifecycle`, AND the `product_lifecycle_events` row inside the same database transaction. The mirror is denormalised data governed by `apply_lifecycle_transition`; no SQLite trigger, no generated column.
3. **V19 uses table rebuilds to remove the V2 inline UNIQUE autoindexes.** `DROP INDEX sqlite_autoindex_*` is rejected by SQLite when the index backs a table-level `UNIQUE` / `PRIMARY KEY` constraint. The V19 body rebuilds `products` and `product_barcodes` (CREATE `_v19` → INSERT → DROP → RENAME), mirroring the V17 `lot_movements` pattern. `PRAGMA defer_foreign_keys = ON` defers FK checks to COMMIT so the DROP is legal.
4. **Atomic lifecycle transitions.** `set_product_lifecycle`, `sync_product_barcodes_lifecycle`, and `INSERT INTO product_lifecycle_events` MUST share one `pool.begin() / tx.commit()` boundary. A failure inside the audit insert rolls back the lifecycle update and the barcode mirror update together; a partial commit is impossible.
5. **`retire` is irreversible and requires reason.** `apply_lifecycle_transition` rejects `target = retired` when `reason` is empty after trim; once committed, no IPC may mutate a retired row other than to read it (`ProductRetiredForMutation`).
6. **Dual-read window for `is_active`.** The legacy `is_active` column is preserved on disk. `set_lifecycle_and_legacy_is_active` writes `lifecycle` + `is_active` together (with `legacy_is_active = None` for retire, leaving `is_active` untouched). Dropping `is_active` is explicitly out of scope; see `design.md §5.4`.

---

## Slice 1 — Pre-flight: confirm anchors and drift baseline

Goal: anchor the diff to today's line numbers before any edit lands so work-unit commits cite exact lines and reviewers can audit without a stale snapshot.

- [x] Read `src-tauri/src/db/migrations.rs` end-to-end and confirm the V2 anchors cited in `explore.md §1`: `products.sku TEXT NOT NULL UNIQUE` at line 84, `product_barcodes.barcode TEXT NOT NULL UNIQUE` at line 101, `expiry_lots` `product_id` FK at line 108, `product_barcodes` `product_id` FK at line 99, the V15 checksum comment at lines 439–445, the V17 `lot_movements` table-rebuild body at lines 604–671, and the last migration index (V18). Record any drift from the design's line numbers inside `openspec/changes/product-lifecycle-reusable-identifiers/apply-progress.md`. <!-- sdd-owner: implementation -->
- [x] Read `src-tauri/src/db/repositories/products.rs` and confirm the anchors cited in `explore.md §2.1`: `repo::archive_product` body at line 350 (`UPDATE products SET is_active = 0, updated_at = …`), `find_active_product_by_barcode_exact` at line 625, `find_active_product_by_sku_exact` at line 660, `RawProductRow` struct, `RawProductBarcodeRow` struct, and the existing `into_response` / `into_search_result` projections. <!-- sdd-owner: implementation -->
- [x] Read `src-tauri/src/services/products.rs` and confirm the anchors cited in `explore.md §2.1` and `design.md §6`: `product_unique_error` at line 65, `barcode_unique_error` at line 78, `services::archive_product` at line 444, `add_barcode` retired guard at line 454, `find_product_by_scan` at line 465, `update_product` body. Record any drift inside `apply-progress.md`. <!-- sdd-owner: implementation -->
- [x] Read `src-tauri/src/services/csv_io.rs` and confirm the anchors cited in `design.md §6.6`: `classify_row` body at line 417, the `DuplicateSku` branch at line 432, the `DuplicateBarcode` branch at line 462, `CsvPreviewRowStatus` enum, `CsvPreviewResponse` summary counters, `export_products_csv` at line 547. <!-- sdd-owner: implementation -->
- [x] Read `src-tauri/src/services/backup_restore.rs` and confirm `REQUIRED_TABLES` at line 30, `export_backup` `VACUUM INTO` body at line 96, `validate_backup` body. Record the baseline list of required tables inside `apply-progress.md`. <!-- sdd-owner: implementation -->
- [x] Read `src/i18n/en/index.ts` (line 365 confirms the unused `Unarchive` key) and `src/i18n/es/index.ts`; capture the exact block boundaries for the `products.*` namespace in both locales so the diff stays mechanical and the `typesafe-i18n` build remains green. <!-- sdd-owner: implementation -->
- [x] Read `src/components/ProductCatalogPage.svelte` (line 65 localStorage key, lines 348–362 displayedProducts, lines 354 hideArchived filter, line 507 archived row class, lines 539–545 Archived badge), `src/components/ProductDetailPage.svelte` (line 156 confirmArchive, lines 305–319 banner copy), `src/components/ProductForm.svelte` (line 92 / line 101 isActive default), `src/components/DashboardPage.svelte` (lines 435–444 modal, line 795 active badge), `src/components/CsvImportPage.svelte` (the duplicate-status rendering). Record the exact line numbers inside `apply-progress.md`. <!-- sdd-owner: implementation -->
- [x] Initialize `openspec/changes/product-lifecycle-reusable-identifiers/apply-progress.md` with the file-scope summary, the per-PR LOC forecast, the design §10 PR boundaries, the locked product decisions D1–D5 from `proposal.md > Confirmed product decisions`, the design-constraint list above (1–6), and the spec-scenario matrix from `specs/caduxo-expiry-tracker/spec.md` as an unfilled template. This file is the apply-phase evidence ledger and MUST exist before any production-code slice lands. <!-- sdd-owner: implementation -->

---

## PR 1 — Backend foundation (~420 net, lands first)

### Slice 2 — V19 migration: `lifecycle` column + partial unique indexes + audit table

Goal: add the `lifecycle` column to `products` and `product_barcodes`, replace the V2 inline `UNIQUE` constraints with same-table partial UNIQUE indexes, and create the `product_lifecycle_events` audit table — all in a single additive migration that does NOT touch any existing migration's SQL bytes.

- [x] Append `(19, "add_product_lifecycle_reusable_identifiers_v19", <V19 SQL body>)` to the `MIGRATIONS` const at `src-tauri/src/db/migrations.rs`. The body MUST start with `PRAGMA defer_foreign_keys = ON;` (deferred until COMMIT so the `DROP TABLE products` step is legal — design constraint 3); rebuild `products` without the inline `UNIQUE` on `sku` (CREATE `products_v19` with `lifecycle TEXT NOT NULL DEFAULT 'active' CHECK(lifecycle IN ('active','archived','retired'))` → INSERT … FROM products → `UPDATE products_v19 SET lifecycle = CASE WHEN is_active = 0 THEN 'archived' ELSE 'active' END WHERE lifecycle = 'active'` → `DROP TABLE products` → `ALTER TABLE products_v19 RENAME TO products`); rebuild `product_barcodes` with the sibling `lifecycle` column mirroring the parent (CREATE `product_barcodes_v19` → INSERT → back-fill `lifecycle` from `(SELECT lifecycle FROM products WHERE products.id = product_barcodes_v19.product_id)` → `DROP TABLE product_barcodes` → RENAME → CREATE); create the two same-table partial UNIQUE indexes `uq_products_sku_active ON products(sku) WHERE lifecycle != 'retired'` and `uq_product_barcodes_barcode_active ON product_barcodes(barcode) WHERE lifecycle != 'retired'` (design constraint 1); create `product_lifecycle_events` with the schema in `design.md §4.1`; create `idx_product_lifecycle_events_product_created ON product_lifecycle_events(product_id, created_at DESC)`. The migration MUST NOT drop `is_active` (design §2.3 / design constraint 6). <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/db/migrations.rs`, add `#[test] fn v19_applies_on_fresh_db()` asserting the total migration count after a fresh `fresh_test_pool()` apply equals `MIGRATIONS.len()` (= 19). <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/db/migrations.rs`, add `#[test] fn v19_adds_lifecycle_column()` asserting `PRAGMA table_info(products)` includes a row with name `lifecycle`, type `TEXT`, `notnull = 1`, `dflt_value = "'active'"`. Symmetric `v19_adds_lifecycle_column_on_product_barcodes` for the barcode table. <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/db/migrations.rs`, add `#[test] fn v19_partial_unique_indexes_exist()` asserting `sqlite_master` contains rows named `uq_products_sku_active` and `uq_product_barcodes_barcode_active` and does NOT contain `sqlite_autoindex_products_1` or `sqlite_autoindex_product_barcodes_1` (the table-rebuild pattern drops the V2 autoindexes implicitly because the rebuild removes the inline `UNIQUE` clauses — design constraint 3). <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/db/migrations.rs`, add `#[test] fn v19_creates_product_lifecycle_events()` asserting the table exists with the eight documented columns (`id`, `product_id`, `event_type`, `from_state`, `to_state`, `actor`, `reason`, `created_at`) and the three CHECK constraints. <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/db/migrations.rs`, add `#[test] fn v19_backfill_is_active_zero_to_archived()` and `v19_backfill_is_active_one_to_active()`: insert a row with `is_active = 0` (resp. `1`) directly via SQL, run V19, assert `lifecycle = 'archived'` (resp. `'active'`). Add `v19_no_row_projects_to_retired_at_backfill()` asserting the union of `lifecycle` values after backfill contains no `'retired'` rows. <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/db/migrations.rs`, add `#[test] fn v19_backfill_product_barcodes_lifecycle_mirrors_parent()` asserting every `product_barcodes.lifecycle` value matches its parent product's `lifecycle` at V19 apply time (design constraint 2 back-fill rule). <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/db/migrations.rs`, add `#[test] fn v19_preserves_expiry_lots_fk_after_products_rebuild()` and `v19_preserves_product_categories_fk_after_products_rebuild()`: after V19, assert every `expiry_lots.product_id` and `product_categories.product_id` resolves to a valid `products.id` (the regression guard for the deferred-FK path used during the rebuild). <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/db/migrations.rs`, add `#[test] fn v19_sku_unique_against_active_but_reusable_after_retire()` and `v19_barcode_unique_against_active_but_reusable_after_parent_retire()`: create a product, assert duplicate SKU is blocked, retire it (via the service helper from Slice 4), then create a new product with the same SKU and assert success (design constraint 1 end-to-end). Symmetric barcode test attaches a barcode, blocks a duplicate, retires the parent, and attaches the same barcode to a different active product and asserts success. <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/db/migrations.rs`, add `#[test] fn v19_audit_row_atomic_with_mutation()`: insert a product, attempt a transition where the audit insert is forced to fail (e.g. `to_state = 'unknown'` violating the CHECK), assert the transaction rolls back and the product's `lifecycle` is unchanged AND no row landed in `product_barcodes` with the orphan lifecycle value (design constraint 4 end-to-end). <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/db/migrations.rs`, add `#[test] fn v19_backup_round_trip_preserves_lifecycle()`: run V19, then `export_backup` via `VACUUM INTO`, then re-import and assert every `products.lifecycle` / `product_barcodes.lifecycle` value is identical and `product_lifecycle_events` rows are present (design constraint 3 round-trip). <!-- sdd-owner: implementation -->
- [x] Run `cargo test --manifest-path src-tauri/Cargo.toml --lib migrations::tests` and confirm the 12 new V19 tests pass. Record the exit code and per-test result inside `apply-progress.md`. <!-- sdd-owner: implementation -->

### Slice 3 — DTOs, user messages, and IPC contract

Goal: introduce the wire and message types so PR 1's IPC handlers (Slice 6) compile against a stable contract and PR 2's frontend wrapper can import them.

- [x] In `src-tauri/src/dto/products.rs`, add `pub enum ProductLifecycle { Active, Archived, Retired }` with `#[serde(rename_all = "snake_case")]` matching the wire shape (`active` / `archived` / `retired`). <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/dto/products.rs`, add `pub enum ProductLifecycleEventType { Archived, Unarchived, Retired }` with `#[serde(rename_all = "snake_case")]` (`archived` / `unarchived` / `retired`). <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/dto/products.rs`, add `pub struct ProductLifecycleEventResponse { id, product_id, event_type, from_state, to_state, actor: Option<String>, reason: Option<String>, created_at }` (all `String` except the enums) with `#[derive(Debug, Clone, Serialize)]`. <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/dto/products.rs`, add `pub struct RetireProductInput { pub id: String, pub reason: String, pub actor: Option<String> }` with `#[derive(Debug, Deserialize)]`. The `reason` field is required and the service layer enforces non-blank-after-trim (design constraint 5). <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/dto/csv_io.rs`, extend `pub enum CsvPreviewRowStatus` with two new variants: `ReleasedSku { existing_product_id: String, existing_sku: String }` and `ReleasedBarcode { existing_product_id: String, existing_barcode: String }`. Each carries the retired row's id + the identifier value so the UI can render a "previously associated with product X" tooltip. <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/dto/csv_io.rs`, extend `pub struct CsvPreviewResponse` summary with `pub released_sku_rows: u32` and `pub released_barcode_rows: u32` counters; `valid_rows` MUST include `ReleasedSku` / `ReleasedBarcode` so the import commits them (design §6.6). <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/services/user_messages.rs`, add `pub enum UserMessage` variants `ProductRetiredForMutation`, `RetireReasonRequired`, `ProductRetiredForBarcode`. Add English + Spanish translations for each (codes `product_retired_for_mutation`, `retire_reason_required`, `product_retired_for_barcode`). <!-- sdd-owner: implementation -->
- [x] Add `#[cfg(test)] mod tests` in `user_messages.rs` asserting every new variant returns a non-empty string for both `Locale::En` and `Locale::Es`, and that `parse_user_message_kind` round-trips each English string back to the originating variant (parity gate). <!-- sdd-owner: implementation -->

### Slice 4 — Repository: lifecycle helpers + mirror invariant + audit read

Goal: add the four new repo helpers that `apply_lifecycle_transition` (Slice 5) and the lifecycle service (Slice 5) call into, plus rewrite the two scanner active-only helpers to filter on `lifecycle = 'active'` so retired products are excluded from operational lookup paths (design constraint 1 + spec `retired products are excluded from operational paths`).

- [x] In `src-tauri/src/db/repositories/products.rs`, add `pub async fn get_product_lifecycle(pool: &SqlitePool, id: &str) -> Result<Option<ProductLifecycle>, sqlx::Error>` — a single-column SELECT scoped to the in-flight transaction (the helper accepts `&mut sqlx::Transaction` so it can be called inside `apply_lifecycle_transition`'s `BEGIN`). <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/db/repositories/products.rs`, add `pub async fn set_product_lifecycle(tx: &mut sqlx::Transaction<'_, Sqlite>, id: &str, lifecycle: ProductLifecycle) -> Result<bool, sqlx::Error>` — the canonical writer used by `set_lifecycle_and_legacy_is_active` (design §5.2). Returns `true` if a row was updated. <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/db/repositories/products.rs`, add `pub async fn sync_product_barcodes_lifecycle(tx: &mut sqlx::Transaction<'_, Sqlite>, product_id: &str, lifecycle: ProductLifecycle) -> Result<u64, sqlx::Error>` — one statement: `UPDATE product_barcodes SET lifecycle = $1 WHERE product_id = $2;` (design constraint 2). The partial unique index on `product_barcodes(barcode) WHERE lifecycle != 'retired'` is unaffected because `barcode` itself does not change. <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/db/repositories/products.rs`, add `pub async fn list_lifecycle_events(pool: &SqlitePool, product_id: &str) -> Result<Vec<ProductLifecycleEventResponse>, sqlx::Error>` — `SELECT … FROM product_lifecycle_events WHERE product_id = $1 ORDER BY created_at DESC, id DESC`. <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/db/repositories/products.rs`, rewrite the existing `find_active_product_by_barcode_exact` and `find_active_product_by_sku_exact` to filter `AND products.lifecycle = 'active'` (replace the existing `AND products.is_active = 1`). The scanner resolver requires no code change; the helper rewrite is sufficient (design §6.3 / spec `scanner resolver does not match a barcode / SKU owned only by a retired product`). <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/db/repositories/products.rs`, add `pub async fn find_by_barcode_exact_including_retired` / `find_by_sku_exact_including_retired` helpers (or extend `find_by_barcode_exact` / `find_by_sku_exact` with a `lifecycle_filter: Option<…>` parameter) so `services::csv_io::classify_row` (Slice 7) and `services::products::find_product_by_scan` can distinguish "matches a retired row only" from "matches an active or archived row". The CSV classify path uses the existing helpers with no lifecycle filter (every row returned), then branches on `lifecycle`. The dashboard's `find_product_by_scan` keeps the existing no-filter behaviour. <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/db/repositories/products.rs`, add `lifecycle: String` field to `RawProductRow` and to `RawProductBarcodeRow`. Project the field through `into_response` / `into_search_result` / `into_barcode_response` so the wire DTOs carry `lifecycle: ProductLifecycle` (snake_case). When the column is `NULL` (legacy backend not yet migrated), default to `Active` to keep backward compatibility with rolling-deploy clients (design §5.3). <!-- sdd-owner: implementation -->
- [x] Add `#[tokio::test]` cases in `db/repositories/products.rs::tests`: `archive_product_writes_lifecycle_event` (asserts `set_lifecycle_and_legacy_is_active` + `sync_product_barcodes_lifecycle` + audit row all share one transaction and the audit row's `to_state = 'archived'`, `reason = NULL`), `unarchive_product_writes_lifecycle_event` (symmetric), `retire_product_writes_lifecycle_event_with_reason` (asserts `reason = 'Discontinued'`, `to_state = 'retired'`, `actor = 'user-1'`), `retire_product_rejects_blank_reason` (asserts validation error and no audit row), `retire_product_rejects_retired_product` (asserts `ProductRetiredForMutation`), `set_lifecycle_and_legacy_is_active_keeps_columns_consistent` (asserts `lifecycle = 'archived'` AND `is_active = 0` after archive; symmetric after unarchive; retire leaves `is_active` untouched), `find_active_*_excludes_retired` (asserts the rewritten helpers return `None` for retired-only inputs), `sync_product_barcodes_lifecycle_propagates_to_owned_rows` (asserts every owned barcode row updates and unrelated products are untouched), `sync_product_barcodes_lifecycle_preserves_retired_rows` (asserts a retired product's barcodes stay retired when a sibling product's lifecycle changes), `lifecycle_event_atomicity` (forced audit-insert failure rolls back `set_product_lifecycle` AND `sync_product_barcodes_lifecycle` — design constraint 4 end-to-end). <!-- sdd-owner: implementation -->

### Slice 5 — Service layer: `apply_lifecycle_transition` + `archive` refactor + `unarchive` / `retire` / `list_lifecycle_events`

Goal: route every lifecycle mutation through one transactional helper that satisfies design constraints 2, 4, and 5; expose the three service-layer functions called by the IPC handlers in Slice 6.

- [x] In `src-tauri/src/services/products.rs`, add `async fn apply_lifecycle_transition(pool: &DbPool, product_id: &str, expected_from: Option<ProductLifecycle>, target: ProductLifecycle, event_type: ProductLifecycleEventType, actor: Option<String>, reason: Option<String>) -> Result<(), AppError>`. The body MUST: (a) `pool.begin()` to start a transaction; (b) call `repo::get_product_lifecycle` inside the tx and validate the precondition; (c) reject `current = retired` with `UserMessage::ProductRetiredForMutation` (design constraint 5); (d) reject `target = retired` when `reason` is empty after trim with `UserMessage::RetireReasonRequired` (design constraint 5); (e) call `repo::set_product_lifecycle` then `repo::sync_product_barcodes_lifecycle` (design constraint 2 — mirror invariant); (f) `INSERT INTO product_lifecycle_events (…)` with the supplied `actor` / `reason` and a freshly generated `id` + `created_at`; (g) `tx.commit().await?`. All three writes share the transaction (design constraint 4). <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/services/products.rs`, refactor the existing `pub async fn archive_product(pool: &DbPool, id: String) -> Result<(), AppError>` to call `apply_lifecycle_transition(pool, &id, Some(ProductLifecycle::Active), ProductLifecycle::Archived, ProductLifecycleEventType::Archived, None, None).await?`. The public signature stays identical so `commands/products.rs::archive_product` does not need a change (design §4.3). `set_lifecycle_and_legacy_is_active` keeps `is_active = 0` in sync inside `apply_lifecycle_transition`. <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/services/products.rs`, add `pub async fn unarchive_product(pool: &DbPool, id: String) -> Result<(), AppError>` calling `apply_lifecycle_transition(pool, &id, Some(ProductLifecycle::Archived), ProductLifecycle::Active, ProductLifecycleEventType::Unarchived, None, None).await?`. Idempotent no-op if already `active`; rejects `retired` via `ProductRetiredForMutation`. <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/services/products.rs`, add `pub async fn retire_product(pool: &DbPool, input: RetireProductInput) -> Result<(), AppError>` calling `apply_lifecycle_transition(pool, &input.id, None, ProductLifecycle::Retired, ProductLifecycleEventType::Retired, input.actor.clone(), Some(input.reason.clone())).await?`. The `expected_from = None` accepts both `active` and `archived`; `apply_lifecycle_transition` enforces the non-blank-reason rule (design constraint 5). `set_lifecycle_and_legacy_is_active` is called with `legacy_is_active = None` so the `is_active` column is NOT rewritten (retired rows remain visible to legacy read paths until the explicit retirement of `is_active` lands later; design §5.2). <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/services/products.rs`, add `pub async fn list_lifecycle_events(pool: &DbPool, product_id: String) -> Result<Vec<ProductLifecycleEventResponse>, AppError>` delegating to `repo::list_lifecycle_events`. <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/services/products.rs`, replace the existing `add_barcode` retired/archived guard at line 454 with the lifecycle-aware check from `design.md §6.4`: read `repo::get_product(…, &input.product_id)`, match on `product.lifecycle` for `Retired` / `Archived` and return `UserMessage::ProductRetiredForBarcode` / `UserMessage::ProductArchivedForBarcode` respectively (preserve the existing archived rejection). <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/services/products.rs`, extend the existing `update_product` flow so a user-driven `is_active` flip (form path) records the matching audit row: if the input toggles `is_active = true → false`, branch into `apply_lifecycle_transition(..., Some(Active), Archived, Archived, None, None)` AFTER the metadata UPDATE commits; symmetric for `false → true` (`Unarchived`). A retire toggle is NOT added to the update form (design §6.8). <!-- sdd-owner: implementation -->
- [x] Add `#[tokio::test]` cases in `services/products.rs::tests`: `archive_product_round_trip` (asserts `lifecycle = 'archived'`, audit row, barcode mirror, `is_active = 0`), `unarchive_product_round_trip`, `retire_product_round_trip`, `retire_product_rejects_retired_product`, `add_barcode_rejects_retired` (asserts `ProductRetiredForBarcode`), `add_barcode_rejects_archived` (asserts legacy archived guard still works), `archive_mirrors_lifecycle_to_owned_barcodes`, `unarchive_mirrors_active_to_owned_barcodes`, `retire_mirrors_retired_to_owned_barcodes_and_releases_value_for_reuse` (asserts the partial index excludes the retired barcode row so a new `add_barcode` with the same value succeeds against an active parent — design constraint 1 + 2 end-to-end), `reuse_released_barcode_succeeds_against_active_parent` (symmetric end-to-end). <!-- sdd-owner: implementation -->

### Slice 6 — IPC wiring + CSV import preview + backup/restore + scanner test

Goal: expose the new IPCs to the frontend (PR 2 picks them up) and apply the lifecycle-awareness to the CSV preview classifier and the backup `REQUIRED_TABLES` list.

- [x] In `src-tauri/src/commands/products.rs`, add `#[tauri::command] pub async fn unarchive_product(state: State<'_, AppState>, id: String) -> Result<(), CommandError>` delegating to `services::products::unarchive_product`. <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/commands/products.rs`, add `#[tauri::command] pub async fn retire_product(state: State<'_, AppState>, input: RetireProductInput) -> Result<(), CommandError>` delegating to `services::products::retire_product`. Map `CommandError` per the existing pattern. <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/commands/products.rs`, add `#[tauri::command] pub async fn list_product_lifecycle_events(state: State<'_, AppState>, product_id: String) -> Result<Vec<ProductLifecycleEventResponse>, CommandError>` delegating to `services::products::list_lifecycle_events`. <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/lib.rs`, register the three new commands in the `invoke_handler` chain alongside the existing `archive_product`. <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/services/csv_io.rs`, rewrite the `classify_row` SKU branch (line 432) and barcode branch (line 462) to be three-state classifiers: if `existing.lifecycle == Retired` emit `ReleasedSku` / `ReleasedBarcode` (the import passes through with an advisory notice); otherwise emit `DuplicateSku` / `DuplicateBarcode` (the import blocks). The "active-or-archived match wins over retired match" rule (when both exist) is enforced by checking `lifecycle != Retired` first in the lookup. The summary counters increment `released_sku_rows` / `released_barcode_rows` accordingly. <!-- sdd-owner: implementation -->
- [x] Add `#[tokio::test]` cases in `services/csv_io.rs::tests`: `classify_row_released_sku` (asserts `ReleasedSku { existing_product_id, existing_sku }` for a SKU matching only a retired row), `classify_row_released_barcode` (symmetric), `classify_row_active_duplicate_sku_wins_over_released` (asserts when both retired AND active rows match, the result is `DuplicateSku`), `preview_summary_counts_released_rows_as_valid` (asserts `valid_rows` includes the released row count and the import commits it). <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/services/backup_restore.rs`, add `"product_lifecycle_events"` to `REQUIRED_TABLES` at line 30 (design §6.7). The audit table is included in every `VACUUM INTO` snapshot because `VACUUM INTO` copies the whole database; the explicit `REQUIRED_TABLES` entry is what makes `validate_backup` accept backups that contain the new table. <!-- sdd-owner: implementation -->
- [x] Add `#[tokio::test]` cases in `services/backup_restore.rs::tests`: `backup_includes_product_lifecycle_events_table` (asserts the required-tables list contains the audit table) and `backup_round_trip_preserves_audit_rows` (asserts an audit row written before backup survives `VACUUM INTO` and re-import). <!-- sdd-owner: implementation -->
- [x] In `src-tauri/src/services/scanner.rs`, no code change required — the repository helpers' lifecycle filter (Slice 4) is sufficient. Add `#[tokio::test]` cases: `retired_product_barcode_does_not_resolve` (asserts `resolve_scanner_code` returns `Unknown { scanned_value }` for a barcode owned only by a retired product), `retired_product_sku_does_not_resolve` (symmetric), `lot_scan_for_retired_product_resolves_with_badge_signal` (asserts the resolved lot carries the parent product's lifecycle field so `LotDetailPage.svelte` can render the `Retired product` badge — spec `lot-code scans for retired products`). <!-- sdd-owner: implementation -->
- [x] Run `cargo test --manifest-path src-tauri/Cargo.toml --lib` and confirm every test added by Slices 2–6 is green. Record the exit code, total test count, and any failing test names inside `apply-progress.md`. <!-- sdd-owner: implementation -->

### Slice 7 — PR 1 verify gate (backend only)

Goal: confirm PR 1's backend is internally consistent and ready for review before opening the PR.

- [x] Run `cargo build --manifest-path src-tauri/Cargo.toml` from `/home/xeworg/Proyectos/Caduxo`; exit code 0; no compiler warnings introduced by the new code. Record the exit code and any warnings touching inside `apply-progress.md`. <!-- sdd-owner: implementation -->
- [x] Run `cargo test --manifest-path src-tauri/Cargo.toml --lib` from `/home/xeworg/Proyectos/Caduxo`; exit code 0; total test count = (previous count) + (V19 ×12 + repo helpers ×10 + services ×10 + CSV ×4 + backup ×2 + scanner ×3) = (previous) + 41. Record the exit code and the per-module result inside `apply-progress.md`. <!-- sdd-owner: implementation -->
- [x] Run `grep -nE "UNIQUE" src-tauri/src/db/migrations.rs` and confirm only the two new `CREATE UNIQUE INDEX … WHERE lifecycle != 'retired'` lines appear (no surviving `TEXT NOT NULL UNIQUE` for `products.sku` or `product_barcodes.barcode`). Record the output inside `apply-progress.md`; treat any surviving inline `UNIQUE` on the two columns as a blocker. <!-- sdd-owner: implementation -->
- [x] Run `grep -nE "sqlite_autoindex_(products|product_barcodes)" src-tauri/src/db/migrations.rs` and confirm zero matches (the rebuild pattern drops the V2 autoindexes implicitly — design constraint 3). Record the output inside `apply-progress.md`. <!-- sdd-owner: implementation -->
- [x] Run `git diff --stat src-tauri/` from `/home/xeworg/Proyectos/Caduxo` and confirm the net human-edited line count for PR 1 is ≤ 400 lines (the design §10 forecast is ~360 production + ~60 test). If the count exceeds 400, escalate via `deliveryStrategy: ask-on-risk` and split into PR 1a (schema + repository + service) and PR 1b (DTOs + IPC + CSV + backup + message variants) per the design §10 escape hatch. Record the `git diff --stat` output inside `apply-progress.md`. <!-- sdd-owner: implementation -->

---

## PR 2 — UI + i18n parity (~420 net, stacks onto PR 1)

### Slice 8 — Frontend types + lib wrappers

Goal: lock the frontend DTO contract and the typed invoke wrappers before any Svelte component edit so `svelte-check` stays green at every intermediate state.

- [x] In `src/lib/products.ts`, add `export type ProductLifecycle = "active" | "archived" | "retired"`; extend `ProductResponse` and `ProductSearchResult` with optional `lifecycle?: ProductLifecycle` (the optional field keeps the older frontend binary working during rolling deployment — design §5.3). <!-- sdd-owner: implementation -->
- [x] In `src/lib/products.ts`, add `export interface ProductLifecycleEventResponse { id: string; product_id: string; event_type: "archived" | "unarchived" | "retired"; from_state: ProductLifecycle; to_state: ProductLifecycle; actor: string | null; reason: string | null; created_at: string }` matching the Rust DTO. <!-- sdd-owner: implementation -->
- [x] In `src/lib/products.ts`, add `export interface RetireProductInput { id: string; reason: string; actor?: string | null }`. <!-- sdd-owner: implementation -->
- [x] In `src/lib/products.ts`, add typed wrappers: `export async function unarchiveProduct(id: string): Promise<void>`, `export async function retireProduct(input: RetireProductInput): Promise<void>`, `export async function listProductLifecycleEvents(productId: string): Promise<ProductLifecycleEventResponse[]>`. Each wrapper invokes the matching Tauri command and returns the unwrapped payload per the existing wrapper conventions. <!-- sdd-owner: implementation -->
- [x] Add a small derived helper in `src/lib/products.ts`: `export function lifecycleOf(p: { lifecycle?: ProductLifecycle; is_active: boolean }): ProductLifecycle` returning `p.lifecycle ?? (p.is_active ? "active" : "archived")` so every UI surface renders the badge consistently against either backend version (design §5.3). <!-- sdd-owner: implementation -->

### Slice 9 — i18n key parity: `en` + `es`

Goal: add every new key listed in `design.md §7.7` to both `src/i18n/en/index.ts` and `src/i18n/es/index.ts` so `typesafe-i18n`'s build enforces parity and `svelte-check` doesn't emit missing-key diagnostics. Wire the existing `products.unarchive` key in `en/index.ts` to the new unarchive IPC (the key exists at line 365 today with no consumer; the Spanish locale tree must add the matching key).

- [x] In `src/i18n/en/index.ts`, add (or wire) keys under `products.*`: `unarchive` (already at line 365, wire to `unarchiveProduct`), `retire`, `retired`, `retireReason`, `retireReasonLabel`, `retireConfirm`, `retireConfirmBody`, `retireIrreversible`, `lifecycleEvents`, `lifecycleEventArchived`, `lifecycleEventUnarchived`, `lifecycleEventRetired`, `retireHistoryNotice`, `lifecycleActive`, `lifecycleArchived`, `lifecycleRetired`, `catalog.showRetired`, `detail.bannerArchived`, `detail.bannerRetired`, `detail.unarchiveConfirm`, `detail.retireConfirm`. <!-- sdd-owner: implementation -->
- [x] In `src/i18n/es/index.ts`, add the matching keys for full parity per `design.md §7.7`. Preserve the alphabetical / grouping ordering already used by each block so the diff stays mechanical. <!-- sdd-owner: implementation -->
- [x] In `src/i18n/en/index.ts` and `src/i18n/es/index.ts`, add CSV-related keys `csv.releasedSku`, `csv.releasedBarcode`, `csv.releasedSkuNotice`, `csv.releasedBarcodeNotice`. <!-- sdd-owner: implementation -->
- [x] In `src/i18n/en/index.ts` and `src/i18n/es/index.ts`, add `dashboard.scanRetired`. <!-- sdd-owner: implementation -->
- [x] Run `npm run i18n:generate` from `/home/xeworg/Proyectos/Caduxo`; exit code 0; regenerated `src/i18n/i18n-types.ts` carries every new key under both locales with no missing-key diagnostics. Record the exit code and the `i18n-types.ts` diff stat inside `apply-progress.md`. <!-- sdd-owner: implementation -->

### Slice 10 — `ProductCatalogPage.svelte`: `Show retired` toggle + badge + bottom-sort

Goal: surface retired products on the catalog list behind a parallel `Show retired` toggle so operators can find history-only products without losing the default operational focus.

- [x] In `src/components/ProductCatalogPage.svelte`, add a new `showRetired` toggle alongside the existing `hideArchived` toggle. Persist the value under `localStorage["caduxo.products.catalog.showRetired.v1"]` (mirror the existing `hideArchived` key pattern at line 65). Default `false`. <!-- sdd-owner: implementation -->
- [x] In `src/components/ProductCatalogPage.svelte`, extend the `displayedProducts` derived value (lines 348–362) with a parallel filter: when `showRetired` is `false`, drop rows whose `lifecycleOf(row) === "retired"`; when `true`, keep them. The existing `hideArchived` filter stays unchanged (independent axis: a user can hide archived but show retired, or vice versa). <!-- sdd-owner: implementation -->
- [x] In `src/components/ProductCatalogPage.svelte`, render a distinct `Retired` badge (using `$LL.products.lifecycleRetired()`) for retired rows alongside the existing `Archived` badge (around lines 539–545). <!-- sdd-owner: implementation -->
- [x] In `src/components/ProductCatalogPage.svelte`, when `showRetired` is on, sort retired rows to the bottom of the default ordering. Prefer pushing the `ORDER BY (lifecycle = 'retired') ASC, …` clause into the SQL at `services/products.rs::search_products` (the catalog list already calls `searchProducts`); fall back to in-memory sort if pushing to SQL touches more than the one caller. <!-- sdd-owner: implementation -->
- [x] Run `npx svelte-check --workspace . --threshold error` from `/home/xeworg/Proyectos/Caduxo`; exit code 0; no missing-key diagnostics. Record the exit code inside `apply-progress.md`. <!-- sdd-owner: implementation -->

### Slice 11 — `ProductDetailPage.svelte`: retire + unarchive flows + lifecycle history + conditional banner

Goal: ship the two new actions (unarchive, retire) on the detail page, the read-only lifecycle history section, and the conditional banner copy so the operator can both transition a product and see what happened in the past.

- [x] In `src/components/ProductDetailPage.svelte`, replace the existing single `confirmArchive` flow (line 156) with three parallel state variables `confirmingArchive`, `confirmingUnarchive`, `confirmingRetire` plus the matching two-step retire state (`retireReason`, `retireSecondConfirm`). The retire flow renders a reason input + a second confirm; submit is disabled until the trimmed reason is non-blank AND the second confirm is acknowledged (spec `retire confirmation requires a reason`). <!-- sdd-owner: implementation -->
- [x] In `src/components/ProductDetailPage.svelte`, add an "Unarchive" button rendered conditionally on `lifecycleOf(productDetail) === "archived"`. Single-step confirm. On click, call `unarchiveProduct(productDetail.id)`, refresh `productDetail`, hide the archived banner. Wires the existing `products.unarchive` i18n key (line 365). <!-- sdd-owner: implementation -->
- [x] In `src/components/ProductDetailPage.svelte`, add a "Retire" button rendered conditionally on `lifecycleOf(productDetail) === "active" || "archived"`. Two-step confirm: first modal asks for a free-text reason (`$LL.products.retireReasonLabel()` / `$LL.products.retireReason()`), submit disabled until the trimmed reason is non-blank; second modal asks for explicit "I understand this is irreversible" confirmation (`$LL.products.retireConfirm()` / `$LL.products.retireConfirmBody()`). The submit button calls `retireProduct({ id, reason, actor: null })` (design constraint 5). <!-- sdd-owner: implementation -->
- [x] In `src/components/ProductDetailPage.svelte`, replace the banner copy at lines 305–319 with conditional copy keyed on `lifecycleOf(productDetail)`: `Archived` → `$LL.products.detail.bannerArchived()`, `Retired` → `$LL.products.detail.bannerRetired()`, `Active` → no banner. The button row beneath the banner adapts: `Archived` shows `[Unarchive] [Retire]`, `Retired` shows no buttons (the only affordance is the read-only history), `Active` shows `[Archive] [Retire]`. <!-- sdd-owner: implementation -->
- [x] In `src/components/ProductDetailPage.svelte`, add a "Lifecycle history" section rendered for every state. Loads `listProductLifecycleEvents(productDetail.id)` on mount and re-loads after each transition. Each row shows event type (`$LL.products.lifecycleEventArchived()` / `…Unarchived()` / `…Retired()`), from→to states, timestamp, actor (when present), reason (when present). Newest first. Read-only — MUST NOT expose any mutation affordance for the events it lists (spec `per-product lifecycle history pane`). <!-- sdd-owner: implementation -->
- [x] Run `npx svelte-check --workspace . --threshold error`; exit code 0; no missing-key diagnostics. Record the exit code inside `apply-progress.md`. <!-- sdd-owner: implementation -->

### Slice 12 — `ProductForm.svelte` + `DashboardPage.svelte`: lifecycle visibility

Goal: surface the read-only lifecycle field on the form and the retired badge on the dashboard modal, plus retire-aware behaviour in the quick product-create path.

- [x] In `src/components/ProductForm.svelte`, render a read-only `lifecycle` field below the existing `isActive` checkbox (around line 92 / line 101). The value is `lifecycleOf(product)` so the lifecycle state is visible during edit. The form's `isActive` checkbox continues to drive `archive` / `unarchive` audit rows via the service path in Slice 5 (`update_product` form-driven `is_active` flip). No new lifecycle controls on the create form — newly created products always start at `active`. <!-- sdd-owner: implementation -->
- [x] In `src/components/DashboardPage.svelte`, render both `Archived` and `Retired` badges (using `$LL.products.lifecycleArchived()` / `$LL.products.lifecycleRetired()`) on the product modal (around lines 435–444 / line 795). Use the `lifecycleOf(row)` helper from Slice 8 so the badge is correct against either backend version. <!-- sdd-owner: implementation -->
- [x] In `src/components/DashboardPage.svelte`, update the quick product-create modal so a scanned / typed SKU or barcode that matches only a retired row produces the same `Unknown` return path the Scanner tab uses (it calls `find_active_product_by_*` which now filters `lifecycle = 'active'`). The user is forced to type a fresh SKU/barcode to create the product. The dashboard scan/search surface continues to include retired rows (via `find_product_by_scan`'s no-filter behaviour) with a `Retired` badge so the operator can see the row in history (spec `dashboard scan/search surface includes retired products with a Retired badge`). <!-- sdd-owner: implementation -->
- [x] Run `npx svelte-check --workspace . --threshold error`; exit code 0; no missing-key diagnostics. Record the exit code inside `apply-progress.md`. <!-- sdd-owner: implementation -->

### Slice 13 — `CsvImportPage.svelte`: `ReleasedSku` / `ReleasedBarcode` badges + counter

Goal: surface the new lifecycle-aware CSV preview statuses so operators can tell "blocked by active/archived" from "passing through because the identifier was previously associated with a retired product".

- [x] In `src/components/CsvImportPage.svelte`, render per-row badges for `ReleasedSku` and `ReleasedBarcode` preview-row statuses with the localized notices from `$LL.csv.releasedSkuNotice()` / `$LL.csv.releasedBarcodeNotice()`. The badges are non-blocking: the row is admitted to the import commit and counted under `released_sku_rows` / `released_barcode_rows` in the summary panel. The "Commit import" button now admits `ReleasedSku` / `ReleasedBarcode` rows alongside `Ok` rows (spec `CSV import preview distinguishes released SKU/barcode rows`). <!-- sdd-owner: implementation -->
- [x] In `src/components/CsvImportPage.svelte`, render the `released_sku_rows` and `released_barcode_rows` counters in the summary panel alongside the existing `duplicate_sku_rows` / `duplicate_barcode_rows` counts. The `valid_rows` total includes the released counts (per design §6.6 CSV preview semantics). <!-- sdd-owner: implementation -->
- [x] Run `npx svelte-check --workspace . --threshold error`; exit code 0; no missing-key diagnostics. Record the exit code inside `apply-progress.md`. <!-- sdd-owner: implementation -->

### Slice 14 — `LotDetailPage.svelte`: `Retired product` badge

Goal: ship the `Retired product` badge on the parent product header in the lot detail view so a lot scan that resolves to a lot whose parent is retired renders the parent state clearly (spec `lot-code scans for retired products`).

- [x] In `src/components/LotDetailPage.svelte`, render a `Retired product` badge (`$LL.products.lifecycleRetired()`) on the parent product header conditional on `lifecycleOf(parentProduct) === "retired"`. The lot movements panel and the lot metadata stay unchanged — lot history is the operator's right regardless of parent lifecycle. <!-- sdd-owner: implementation -->
- [x] Run `npx svelte-check --workspace . --threshold error`; exit code 0; no missing-key diagnostics. Record the exit code inside `apply-progress.md`. <!-- sdd-owner: implementation -->

### Slice 15 — PR 2 verify gate

Goal: confirm PR 2's frontend is internally consistent and ready for review before opening the PR.

- [x] Run `npx svelte-check --workspace . --threshold error`; exit code 0; no missing-key diagnostics emitted by `typesafe-i18n`; no Svelte `a11y_*` warnings introduced by the new markup. Record the exit code and any output mentioning `ProductCatalogPage`, `ProductDetailPage`, `ProductForm`, `DashboardPage`, `CsvImportPage`, `LotDetailPage`, or `lib/products.ts` inside `apply-progress.md`. <!-- sdd-owner: implementation -->
- [x] Run `npm run build` from `/home/xeworg/Proyectos/Caduxo`; exit code 0; `prebuild` regenerates `src/i18n/i18n.ts` and `vite build` exits green. Record the exit code and the bundle-size delta inside `apply-progress.md`. <!-- sdd-owner: implementation -->
- [x] Run `git diff --stat src/ src-tauri/` (PR 2 only — exclude PR 1's diff) from `/home/xeworg/Proyectos/Caduxo`; confirm the net human-edited line count for PR 2 is ≤ 400 lines (design §10 forecast is ~300 UI + ~100 i18n + ~20 lib wrappers). If the count exceeds 400, escalate via `deliveryStrategy: ask-on-risk` and split into PR 2a (catalog / detail / dashboard / form / `lib/products.ts` / i18n keys) and PR 2b (CSV import page only — wire to the backend contract shipped in PR 1) per the design §10 escape hatch. Record the `git diff --stat` output inside `apply-progress.md`. <!-- sdd-owner: implementation -->

---

## PR 3 — Verification + review fixes (~140 net, stacks onto PR 2)

### Slice 16 — Manual smoke matrix

Goal: exercise every spec scenario end-to-end inside `npm run tauri dev` so the change can be archived with a receipt. The 16-step matrix below is the canonical checklist; record the outcome (`pass` / `fail` / `partial` + the actual observed value) for each row inside `openspec/changes/product-lifecycle-reusable-identifiers/verify-report.md`. Steps that require a desktop runtime are marked `[ ]` because they can only be ticked during the verify phase, not from a headless shell.

- [ ] M1 — Archive via UI: archive an active product from `ProductDetailPage.svelte`; confirm `lifecycleOf(product)` reads `archived`, the lifecycle history pane shows the audit row with `from_state = active`, `to_state = archived`, `reason = null`, and the `Archived` badge renders on the catalog row when `Show retired` is off. <!-- sdd-owner: implementation -->
- [ ] M2 — Unarchive via UI: unarchive the archived product via the new "Unarchive" button; confirm `lifecycleOf(product)` reads `active`, the lifecycle history pane shows the `unarchived` audit row, and the catalog row no longer carries the `Archived` badge. <!-- sdd-owner: implementation -->
- [ ] M3 — Retire active product with reason: retire the active product via the two-step retire flow with `reason = "Discontinued by supplier"`; confirm `lifecycleOf(product)` reads `retired`, the lifecycle history pane shows the `retired` audit row with the supplied reason, and the catalog row is hidden by default (Show retired off) or renders the `Retired` badge at the bottom when the toggle is on. <!-- sdd-owner: implementation -->
- [ ] M4 — Retire archived product with reason: archive an active product first, then retire it; confirm `from_state = archived`, `to_state = retired`, reason persisted. <!-- sdd-owner: implementation -->
- [ ] M5 — Retire submit disabled with blank reason: open the retire modal with `reason = ""` (or whitespace-only); confirm the submit button is disabled and no IPC fires. <!-- sdd-owner: implementation -->
- [ ] M6 — Retired product cannot be edited / archived / unarchived / barcode-attached: attempt archive / unarchive / update via the form / `add_barcode` on the retired product; confirm each attempt returns `ProductRetiredForMutation` (or the matching barcode guard), the product's lifecycle is unchanged, and no `product_lifecycle_events` row is written. <!-- sdd-owner: implementation -->
- [ ] M7 — Catalog list with `Show retired` off: confirm retired products are hidden from the catalog list. <!-- sdd-owner: implementation -->
- [ ] M8 — Catalog list with `Show retired` on: enable the toggle, reload the page, confirm the localStorage key `caduxo.products.catalog.showRetired.v1` is `true`, retired products are listed at the bottom with the `Retired` badge. <!-- sdd-owner: implementation -->
- [ ] M9 — Dashboard search of retired SKU: type the retired product's SKU into the dashboard scan/search field; confirm the row surfaces with a `Retired` badge and no scanner side-effect (sale / registration / stock-out) is triggered. <!-- sdd-owner: implementation -->
- [ ] M10 — Scanner resolution of retired SKU: scan the retired SKU via `ScannerPage.svelte`; confirm `resolve_scanner_code` returns `Unknown { scanned_value }` (design constraint 1 + spec `scanner resolver does not match a SKU owned only by a retired product`). <!-- sdd-owner: implementation -->
- [ ] M11 — Scanner resolution of retired barcode: scan the retired product's barcode; confirm `Unknown { scanned_value }` (spec `scanner resolver does not match a barcode owned only by a retired product`). <!-- sdd-owner: implementation -->
- [ ] M12 — Scanner resolution of lot under retired parent: scan a lot whose `batch_code` belongs to a lot whose parent is retired; confirm the Scanner tab opens the lot detail view, the parent product header renders the `Retired product` badge, and the lot movements panel lists every movement including those recorded before the parent was retired (spec `lot-code scans for retired products`). <!-- sdd-owner: implementation -->
- [ ] M13 — CSV import preview of retired SKU: create a CSV whose row contains a SKU matching a retired product; preview the import; confirm the row's status is `ReleasedSku`, the per-row badge renders the `$LL.csv.releasedSkuNotice()` localized text, and committing the import creates the new product with `lifecycle = active`. <!-- sdd-owner: implementation -->
- [ ] M14 — CSV import preview of active SKU collision: create a CSV whose SKU matches an active product; confirm the row's status is `DuplicateSku` and the row is NOT committed. <!-- sdd-owner: implementation -->
- [ ] M15 — Migration on a fresh database: reset the local DB, run `npm run tauri dev`, confirm V1–V19 apply cleanly, total migration count = 19, no row projects to `retired`. <!-- sdd-owner: implementation -->
- [ ] M16 — Migration on a populated database: back up an existing populated database with archived and active rows, run V19, confirm every `is_active = 0` row projects to `lifecycle = 'archived'`, every `is_active = 1` row projects to `lifecycle = 'active'`, no row projects to `lifecycle = 'retired'`, the `products` and `product_barcodes` tables round-trip via `VACUUM INTO` without schema drift, and the `product_lifecycle_events` table is included in the backup snapshot. <!-- sdd-owner: implementation -->

### Slice 17 — Verify report + archive

Goal: write the parent gate input and close out the SDD lifecycle so the change can move to `openspec/changes/archive/<date>-product-lifecycle-reusable-identifiers/`.

- [x] Write the verify report at `openspec/changes/product-lifecycle-reusable-identifiers/verify-report.md` summarising: (a) the automated gates from Slice 7 (PR 1) and Slice 15 (PR 2), (b) the four grep gates (`UNIQUE` surviving, `sqlite_autoindex_products_*`, `is_active` projection drift, `lifecycle`/`audit` coverage), (c) the M1–M16 manual-smoke outcomes from Slice 16, (d) the spec-scenario coverage matrix from `specs/caduxo-expiry-tracker/spec.md` marked `pass` / `fail` / `partial` per scenario (every `#### Scenario` block under the lifecycle requirements). The verify report is the parent gate's input; it MUST reference every checklist item above with the concrete evidence (command + exit code + observed output) and link to `apply-progress.md` as the evidence ledger. <!-- sdd-owner: implementation -->

---

## Parent actions (lifecycle gates and review)

- [ ] Before PR 1 apply: confirm the chain strategy is `stacked-to-main` (PR 1 lands to `main`, PR 2 stacks onto PR 1, PR 3 stacks onto PR 2). If `feature-branch-chain` is preferred instead, update the chain strategy before apply; do NOT invent a `size:exception` without explicit user acceptance. The parent preflight has already locked `stacked-to-main`; this is the explicit, documented confirmation. <!-- sdd-owner: parent -->
- [ ] Open PR 1 against `main` once `cargo test --lib` is green (Slice 7) and Slice 7's `git diff --stat` is ≤ 400 net lines. Merge PR 1 only after Slice 7's automated gates pass and PR 1's bounded review is filed. If PR 1's `git diff --stat` exceeds 400 net lines during implementation, escalate via `deliveryStrategy: ask-on-risk` and split into PR 1a (schema + repository helpers + service-layer lifecycle methods + audit) and PR 1b (DTOs, IPC wiring, CSV preview, backup/restore, message variants) before merging — do NOT silently merge an over-budget PR. <!-- sdd-owner: parent -->
- [ ] After PR 1 merges: rebase PR 2 onto `main` (PR 2 now resolves the IPC contract shipped in PR 1). Open PR 2 against `main` once `npx svelte-check` and `npm run build` are green (Slice 15) and Slice 15's `git diff --stat` is ≤ 400 net lines. If PR 2 alone exceeds 400 net lines, escalate via `deliveryStrategy: ask-on-risk` and split into PR 2a (catalog / detail / dashboard / form / `lib/products.ts` / i18n keys) and PR 2b (CSV import page only — wire to the backend contract shipped in PR 1) before merging. <!-- sdd-owner: parent -->
- [ ] After PR 2 merges: rebase PR 3 onto `main`. PR 3 is the verify-report slice; merge it as soon as the Slice 16 manual-smoke matrix is recorded inside `verify-report.md` and the Slice 17 verify-report gate is filed. <!-- sdd-owner: parent -->
- [ ] Start or reuse a bounded review for the change covering at minimum: (a) the V19 rebuild pattern vs `ALTER TABLE ADD COLUMN` (the chosen rebuild is required because `DROP INDEX sqlite_autoindex_*` cannot drop autoindexes backing inline `UNIQUE` constraints — design constraint 3); (b) the same-table partial-index predicate correctness (`products(sku) WHERE lifecycle != 'retired'`, `product_barcodes(barcode) WHERE lifecycle != 'retired'` — no cross-table `EXISTS`, design constraint 1); (c) the `product_barcodes.lifecycle` mirror invariant (design constraint 2 — `apply_lifecycle_transition` runs `set_product_lifecycle` + `sync_product_barcodes_lifecycle` + audit insert in one transaction; verified by `lifecycle_event_atomicity`); (d) the atomic lifecycle transition contract (design constraint 4 — failure inside the audit insert rolls back the lifecycle update AND the barcode mirror update); (e) the `retire` irreversible + reason-required contract (design constraint 5 — `apply_lifecycle_transition` rejects blank reason and rejects any mutation of a retired row); (f) the CSV import three-state classifier correctness (`DuplicateSku` vs `ReleasedSku`, `DuplicateBarcode` vs `ReleasedBarcode`, active-or-archived match wins over retired match); (g) the `Show retired` toggle's bottom-sort behaviour and localStorage persistence; (h) the scanner resolver's `lifecycle = 'active'` filter (retired products no longer match); (j) the lot-scan independence rule (lot resolution does NOT consult parent lifecycle; the `Retired product` badge is rendered on the lot detail parent header); (k) the dual-read window for `is_active` (legacy read paths still work; `is_active` is NOT dropped in this change); (l) the i18n parity between `en` and `es` for every new key in `design.md §7.7`. <!-- sdd-owner: parent -->
- [ ] After all PRs land and the bounded review passes, archive the SDD artifacts (`proposal.md`, `specs/...`, `design.md`, `explore.md`, this `tasks.md`, `apply-progress.md`, `verify-report.md`) per the project's OpenSpec lifecycle into `openspec/changes/archive/<date>-product-lifecycle-reusable-identifiers/`; mark the change `applied` and close out the `sdd/product-lifecycle-reusable-identifiers/*` Engram topic keys once the bounded review receipt is filed. <!-- sdd-owner: parent -->